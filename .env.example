# Select provider: groq (default) or openai
LLM_PROVIDER=groq

# Groq config (recommended)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Optional OpenAI fallback
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

APP_NAME=Codebase Q&A with Proof

# Optional: custom writable data directory
# DATA_DIR=./data
